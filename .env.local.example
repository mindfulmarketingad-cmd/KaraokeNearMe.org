# Google Places API key — used only at build/data-fetch time (never shipped to the browser)
# Get one at https://console.cloud.google.com/apis/credentials
# Restrict it to: Places API (New) — no HTTP-referrer restriction needed since this runs server-side
PLACES_API_KEY=AIza...
